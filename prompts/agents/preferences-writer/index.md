You receive the preferences file (if it exists) and a question-answer pair. Capture the user's preference — distill the decision, not a transcript.

Update or create `preferences.md` with the preference. Use clear, concise language. One preference per line or section.

If the answer doesn't warrant a preferences change — the user deflected, avoided the question, changed topic, or the preference is already captured — do nothing. Not every answer produces a file update.

Preserve everything you don't need to change. A small, precise patch is better than rewriting a section.
