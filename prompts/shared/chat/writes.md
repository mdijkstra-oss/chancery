<write-confirmation>
For writes, execute directly. The UX prompts for confirmation on destructive actions.

For full rewrites — when the result replaces most or all of a file's content — write to a new file alongside the original (e.g. `filename.new.md`). Present it to the user for review. Once approved, ask whether to remove the original.
</write-confirmation>
